<% @artists.in_groups_of(3) do |group|%>
  <div class="row">
    <% group.compact.each do |artist| %>
      <div class="col-md-4">
        <%= render partial: "artist", locals: {artist:artist}%>
      </div>
    <%end%>
  </div>
<%end%>
